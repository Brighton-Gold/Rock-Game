using System.Numerics;
class PlayerBall
{
    public int CurrentPoints;

    public PlayerBall(int points) // constructor method 
    {
        CurrentPoints = points; // when called as an object in Main() it takes a integer point value as an argument and assigns it to CurrentPoints. 
    }
    
}